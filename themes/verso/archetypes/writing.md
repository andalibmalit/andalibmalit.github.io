+++
title = "{{ replace .Name "-" " " | title }}"
date = {{ .Date }}
summary = ""        # one line; shown in the Posts list (falls back to description)
standfirst = ""     # muted lead line under the post title (falls back to description)
description = ""     # meta description / Open Graph
# draft = true
+++

Post body in Markdown. Headings, blockquotes, footnotes, and images are all styled.
